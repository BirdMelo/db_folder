{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "Fake API com json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db_email.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
